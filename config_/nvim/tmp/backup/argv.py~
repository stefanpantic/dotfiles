import sys

def main():
# sys.argv is a list, so we can iterate over it
    if 2 != len(sys.argv):
        print("missing command-line argument")
        exit(1)

    for s in sys.argv:
        for c in s:
            print (c + " ", end="")
    exit(0)

if "__main__" == __name__:
    main()

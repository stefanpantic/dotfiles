import os
import sys
import random

num_map = dict()

r = 0
while 99 != r:
    num_map[r] = num_map.get(r, 0) + 1;
    r = random.randrange(0, 100)

for (x,y) in num_map.items():
    print("%d, %d" % (x, y))

